let html = `<div class="torrent-install">
    <div class="torrent-install__details">
        <div class="torrent-install__title">#{torrent_install_need}</div>
        <div class="torrent-install__descr">#{torrent_install_text}</div>
    </div>
</div>`

export default html
